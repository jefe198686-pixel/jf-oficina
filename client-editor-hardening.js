// JF Oficina v0.19.6 — edição de clientes estável e unificada
(function(){
 const ids=['clientId','clientCode','clientName','clientDoc','clientIE','clientPhone','clientEmail','clientCity','clientFarm','clientAddress','clientBirth','clientObs'];
 const by=id=>document.getElementById(id),txt=v=>String(v??'').normalize('NFC');
 function valuesFor(c){return {clientId:c?.id||'',clientCode:c?(c.matricula||c.codigo_cti||c.id):nextClientCode(),clientName:txt(c?.nome||''),clientDoc:txt(c?.documento||c?.cpf_cnpj||''),clientIE:txt(c?.ie||c?.inscricao_estadual||''),clientPhone:txt(c?.telefone||c?.fone||''),clientEmail:txt(c?.email||''),clientCity:txt(c?.cidade||''),clientFarm:txt(c?.fazenda||''),clientAddress:txt(c?.endereco||c?.logradouro||''),clientBirth:txt(c?.data_nascimento||c?.data_abertura||''),clientObs:txt(c?.observacoes||'')}}
 function apply(vals){ids.forEach(id=>{const el=by(id);if(el)el.value=vals[id]??''})}
 function clear(){const f=by('clientForm');if(f)f.reset();apply(Object.fromEntries(ids.map(id=>[id,''])))}
 function harden(){const f=by('clientForm');if(!f)return;f.autocomplete='off';ids.forEach((id,i)=>{const el=by(id);if(!el)return;el.autocomplete='off';el.setAttribute('data-lpignore','true');el.setAttribute('data-form-type','other');if(id!=='clientId')el.name='jf_client_field_'+i});const n=by('clientName');if(n){n.readOnly=false;n.disabled=false;n.spellcheck=true;n.setAttribute('autocorrect','on');n.setAttribute('autocapitalize','words')}}
 function findStored(id){const sid=String(id||'');const custom=(S.custom?.clientes||[]).find(x=>String(x.id)===sid||String(x.matricula||'')===sid||String(x.codigo_cti||'')===sid);const base=(S.clientes||[]).find(x=>String(x.id)===sid||String(x.matricula||'')===sid||String(x.codigo_cti||'')===sid);return {custom,base}}
 function cleanupDuplicateFor(id){const sid=String(id||'');const base=(S.clientes||[]).find(x=>String(x.id)===sid||String(x.matricula||'')===sid||String(x.codigo_cti||'')===sid);if(!base)return;const dup=(S.custom?.clientes||[]).filter(x=>String(x.id)===String(base.id)||String(x.matricula||'')===String(base.matricula||base.codigo_cti||'')||String(x.codigo_cti||'')===String(base.codigo_cti||base.matricula||''));for(const d of dup){Object.keys(d).forEach(k=>{if(d[k]!==''&&d[k]!=null)base[k]=d[k]})}S.custom.clientes=(S.custom.clientes||[]).filter(x=>!dup.includes(x))}
 function openStable(id=''){harden();clear();if(id)cleanupDuplicateFor(id);fillClientSelects();const c=id?clientBy(id):null,vals=valuesFor(c);apply(vals);const title=by('clientTitle');if(title)title.textContent=c?`Cliente — ${txt(c.nome||'')}`:'Novo cliente';const rel=by('clientRelations');if(rel)rel.classList.toggle('hidden',!c);if(c)renderClientRelations(c.id);const dlg=by('clientDlg');if(dlg&&!dlg.open)dlg.showModal();requestAnimationFrame(()=>apply(vals));setTimeout(()=>apply(vals),80)}
 async function saveStable(e){e.preventDefault();e.stopImmediatePropagation();const id=by('clientId').value;const fields={matricula:txt(by('clientCode').value),codigo_cti:txt(by('clientCode').value),nome:txt(by('clientName').value),documento:txt(by('clientDoc').value),cpf_cnpj:txt(by('clientDoc').value),ie:txt(by('clientIE').value),inscricao_estadual:txt(by('clientIE').value),telefone:txt(by('clientPhone').value),email:txt(by('clientEmail').value),cidade:txt(by('clientCity').value),fazenda:txt(by('clientFarm').value),endereco:txt(by('clientAddress').value),logradouro:txt(by('clientAddress').value),data_nascimento:txt(by('clientBirth').value),observacoes:txt(by('clientObs').value)};S.custom=S.custom||{};S.custom.clientes=S.custom.clientes||[];let c,created=false;if(id){const st=findStored(id);if(st.base){Object.assign(st.base,fields);c=st.base;cleanupDuplicateFor(st.base.id)}else if(st.custom){Object.assign(st.custom,fields);c=st.custom}else{c={id,...fields};S.custom.clientes.unshift(c)}}else{created=true;c={id:uid('C'),...fields};S.custom.clientes.unshift(c)}log('cliente',c.id,created?'Cliente criado':'Cliente alterado');await saveDB();render();const dlg=by('clientDlg');if(dlg?.open)dlg.close();clear();if(typeof jfSyncNow==='function')setTimeout(()=>jfSyncNow('auto'),50);if(typeof pushCentralSync==='function')pushCentralSync().catch(()=>{});if(by('osDlg')?.open){fillClientSelects();by('osClient').value=c.id;work.cliente_id=c.id;renderEquipments()}}
 function install(){harden();window.openClient=openStable;const f=by('clientForm');if(f)f.onsubmit=saveStable;const close=()=>{const d=by('clientDlg');if(d?.open)d.close();clear()};if(by('closeClient'))by('closeClient').onclick=close;if(by('clientCancel'))by('clientCancel').onclick=close}
 addEventListener('DOMContentLoaded',()=>setTimeout(install,1300));setTimeout(install,2400);
})();